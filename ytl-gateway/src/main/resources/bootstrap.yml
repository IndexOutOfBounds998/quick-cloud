spring:
  application:
    name: gateway
  cloud:
    config:
      name: gateway
      label: master
      profile: test
      enabled: true
      fail-fast: false
      retry:
        max-attempts: 6
    bus:
      trace:
        enabled: true